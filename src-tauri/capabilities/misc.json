{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "windows": ["main"],
  "permissions": [
    "core:app:allow-set-app-theme",
    "dialog:default",
    "opener:default"
  ]
}
